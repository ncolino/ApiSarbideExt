version: 0.0
os: linux
files:
  - source: .
    destination: /srv/www/ApiSarbideExt
  - source: config/nginx.conf
    destination: /etc/nginx/nginx.conf
hooks:
  BeforeInstall:
    - location: test/mueve.sh    
      timeout: 300
      runas: root
